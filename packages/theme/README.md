# Theme Source

This directory is the shared Nexum theme source.

It started from the local `no-style-please` fork and now contains the generic layout, navigation, typography, and shared component styling used by the site. The Effect generator copies these files into the generated Jekyll source under `site/` before every build.
