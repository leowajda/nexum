import { sourceNotesProjectAdapter } from "./build.js"
import type { ProjectDefinition } from "../project.js"

export const sourceNotesProjectDefinition: ProjectDefinition = {
  adapter: sourceNotesProjectAdapter
}
