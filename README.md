# Local Build

1. Clone the repo with submodules: `git clone --recurse-submodules <repo-url>`.
1. Install dependencies: `pnpm install`.
1. Generate the site data: `pnpm generate`.
1. Build the site: `pnpm build:site`.
1. Preview locally: `pnpm serve` and open `http://127.0.0.1:4000`.
